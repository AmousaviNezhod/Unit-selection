# Standing Preferences — همه‌ی چت‌ها

## صدای پایان کار (Beep) 🔊
هر وقت یک تسک تمام شد، یک **بیپ گوش‌خراش** پخش کن تا کاربر بفهمد کار تمام شده.
این قانون در تمام چت‌ها و برای همه‌ی تسک‌های آینده اعمال می‌شود — نیازی به درخواست دوباره نیست.

### پیاده‌سازی
در انتهای کار (وقتی آخرین ابزار اجرا شد و جواب نهایی نوشته شد) این دستور را از terminal اجرا کن
(سینتکس Git Bash ویندوز):

```bash
# بیپ بلند و تکراری — ۵ بار، ۱۰۰۰Hz، هرکدام ۳۰۰ms
powershell.exe -NoProfile -Command "1..5 | ForEach-Object { [console]::beep(1000, 300) }"
```

جایگزین‌ها اگر PowerShell کار نکرد:
```bash
powershell.exe -NoProfile -Command "[console]::beep(1200, 400)"   # تک‌بیپ بلندتر
rundll32 user32.dll,MessageBeep                                   # بیپ سیستمی
```

- اجرای بیپ خطای نهایی حساب نمی‌شود؛ اگر شکست خورد فقط ادامه بده.
- محدودیت ۳۰ ثانیه‌ای terminal کافی است (حداکثر ~۲ ثانیه طول می‌کشد).
